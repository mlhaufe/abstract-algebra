/**
 * 
 * https://ncatlab.org/nlab/show/coalgebra
 * 
 * CoMultiplication
 *     Δ: A → A ⊗ A
 * 
 * a coalgebra is a monoid in the Vect^{op} category, just as an algebra is a monoid in Vect category.
 * 
 * CoUnit
 *     e:A → k
 * 
 * coassociative?
 * 
 * Also called a "CoAlgebra" ?
 */
interface CoMonoid<T> {}

export default CoMonoid